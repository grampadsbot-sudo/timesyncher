# Filters

List tabs (Restaurants / Stores / The Rest):

- **All areas**
- **All types** — Rest type chips (`Os`) show only types present on real Things in that Rest list (area-filtered, exclude `__tsLiveFill`). Restaurant / store chips = tags on Things in that list (`tags-chips.md`).

Chip toggles in live shared renderer. Empty copy when nothing matches: see `empty-states.md`.
