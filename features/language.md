# Language

TREK login/landing chrome in `index-BKun7ofk.js`:

- `aria-label="Change language"`
- `aria-label="Select language"`

Not an `aria-label` on the shared vacation-3 trip header (that header is Config Options / PDFs / Order Keepsakes). Do not invent extra locale pickers.
