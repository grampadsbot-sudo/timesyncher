# Keepsake layouts

Product has **two** layouts only. Export = **Style two**.

| UI | Report name | URL |
| --- | --- | --- |
| Style one | `keepsake` | `/api/pdf/shared/{token}/report/keepsake.pdf` |
| Style two | `keepsake-style-2` | `/api/pdf/shared/{token}/report/keepsake-style-2.pdf` |

Click path: **PDFs** → **Keepsakes ▸** → **Style two**.

HTML preview: `?printMode=report&pdfReport=keepsake-style-2`.

SoT: `bot-admin/messages/time-syncher/style-2-journey-book-standard`.
Skill (in flight): `skills/style-2-journey-book-export`.

A Cursor-built HTML book is a third layout and is product drift.
