# Order Keepsakes

Shared-trip header button `aria-label="Order Keepsakes"` is **in** the live vacation-3 renderer. Icon always; text **Order Keepsakes** only when that control is selected. Purchase/order chrome (`Mt("keepsakes")`).

Separate from **PDFs → Keepsakes ▸** Style one / Style two export.
