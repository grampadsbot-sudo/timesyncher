# Shared header chrome

Live shared vacation-3 header (`index-BKun7ofk.js`, `data-trip-view-root`):

- Brand: **TimeSyncher Vacation** + trip title + day count
- **Config Options** (`aria-label`)
- **PDFs** (`aria-label`)
- **Order Keepsakes** (`aria-label`)

Not in this header dump: voice-note recorder, media toggle, QR toggle.
